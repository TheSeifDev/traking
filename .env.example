# Server-only Resend configuration. Never expose these values to the browser.
# Replace the empty value with your real Resend API key in local/Vercel environment settings.
RESEND_API_KEY=

# Use a sender verified in your Resend account/domain.
RESEND_FROM_EMAIL=

# Optional reply-to address.
RESEND_REPLY_TO=
